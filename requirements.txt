# Core dependencies for ContractLens Streamlit demo (HF Spaces)
# Heavy ML packages (torch, transformers, chromadb) are excluded —
# they're only needed for the fine-tuning and vector-store features.

streamlit>=1.35.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
typing-extensions>=4.8.0

# LLM + orchestration
litellm>=1.0.0
langgraph>=0.2.0
langchain-core>=0.1.0

# Utilities
python-dotenv>=1.0.0
tiktoken>=0.5.0
